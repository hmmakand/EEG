# python3.11 -m venv .venv
# source .venv/bin/activate

# python -m pip install --upgrade pip

# python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126 # for cuda 12.6
# python -m pip install tensorboard

# pip install hydra-core --upgrade
# pip install mne
# pip install moabb
# pip install braindecode
# pip install ipykernel
# pip install ipywidgets



# cat > pyproject.toml <<'EOF'
# [build-system]
# requires = ["setuptools>=68", "wheel"]
# build-backend = "setuptools.build_meta"

# [project]
# name = "eeg-bci-benchmark"
# version = "0.1.0"
# description = "EEG-BCI benchmark for motor imagery classification"
# requires-python = ">=3.10"

# [tool.setuptools.packages.find]
# where = ["src"]
# EOF

# python -m pip install -e . --no-deps
# python -m pip install lightning
# pip install -U pytest

# Persistent homology for manuscript-candidate graph features.
# ripser==0.6.15
# pip install ripser
# mypy==2.3.1
# pip install mypy

# pip install torch_geometric
# pip install -U mne-connectivity