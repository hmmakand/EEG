# @package _global_
experiment_name: within_subject_full
# Full experiment preset: real BCI IV 2a data + dataset-specific preprocessing.
defaults:
  - override /dataset: bcic_iv_2a
  - override /preprocessing: bcic_iv_2a
  - override /training: default

training:
  max_epochs: 10
